/* 页面整体布局 */
page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
  background: transparent;
  overflow: hidden;
}

.page-bg {
  /* Skyline 下 fixed 背景层更容易和滚动容器发生层级/绘制异常，这里改为 absolute。 */
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.page-bg-mask {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.08) 0%,
    rgba(255, 255, 255, 0.14) 45%,
    rgba(255, 255, 255, 0.22) 100%
  );
}

.page-scroll {
  height: 100vh;
  position: relative;
  z-index: 1;
  overflow: hidden;
}

.container {
  height: 100vh;
  padding: 75px 0 0 0;
  display: flex;
  flex-direction: column;
  position: relative;
  min-height: 0;
  box-sizing: border-box;
  overflow: hidden;
}

.pull-refresh-tip {
  position: absolute;
  top: calc(env(safe-area-inset-top) + 12rpx);
  left: 50%;
  transform: translateX(-50%);
  z-index: 3;
  display: flex;
  align-items: center;
  padding: 14rpx 22rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, rgba(255, 251, 242, 0.98) 0%, rgba(239, 248, 255, 0.98) 100%);
  border: 1rpx solid rgba(126, 159, 209, 0.18);
  box-shadow: 0 8rpx 22rpx rgba(62, 91, 135, 0.12);
}

.pull-refresh-tip__spinner {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  border: 3rpx solid rgba(74, 144, 226, 0.18);
  border-top-color: #ffb300;
  border-right-color: #4a90e2;
  box-sizing: border-box;
  animation: pull-refresh-spin 0.9s linear infinite;
  flex-shrink: 0;
  margin-right: 12rpx;
}

.pull-refresh-tip__text {
  font-size: 24rpx;
  font-weight: 600;
  line-height: 1.2;
  color: #516882;
  white-space: nowrap;
}

@keyframes pull-refresh-spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.top-container {
  width: 100%;
  display: flex;
  flex: 1;
  flex-direction: column;
  position: relative;
  z-index: 1;
  justify-content: flex-start;
  min-height: 0;
  overflow: hidden;
  /* 预留底部栏自身高度，并给中间面板与底部栏之间留出更舒展的间距。 */
  padding-bottom: 116rpx;
  box-sizing: border-box;
}

.bottom-container {
  /* 放在页面容器内绝对定位，避免 viewport fixed 在 Skyline 真机上的不稳定表现。 */
  position: absolute;
  left: 20rpx;
  right: 20rpx;
  bottom: 0;
  width: auto;
  display: flex;
  align-items: center;
  padding: 10rpx 18rpx 8rpx;
  box-sizing: border-box;
  flex-shrink: 0;
  z-index: 2;
  border-radius: 22rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.96) 0%, rgba(248, 251, 255, 0.96) 100%);
  border: 1rpx solid rgba(126, 159, 209, 0.16);
  box-shadow: 0 8rpx 22rpx rgba(62, 91, 135, 0.1);
}

.content-panel {
  flex: 1;
  min-height: 0;
  /* 让内部 scroll-view 能拿到剩余高度，避免在 Skyline 下被算成 auto/0。 */
  height: 0;
  display: block;
  margin: 10rpx 20rpx 20rpx 20rpx;
  border-radius: 22rpx;
  border: 1rpx solid rgba(126, 159, 209, 0.16);
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 251, 255, 0.98) 100%);
  box-shadow: 0 8rpx 22rpx rgba(62, 91, 135, 0.1);
  box-sizing: border-box;
  overflow: hidden;
}

.nav-btn {
  flex: 1;
  min-height: 72rpx;
  padding: 10rpx 14rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 999rpx;
  border: 1rpx solid rgba(126, 159, 209, 0.16);
  box-shadow: 0 4rpx 12rpx rgba(86, 115, 156, 0.06);
  box-sizing: border-box;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.nav-btn + .nav-btn {
  margin-left: 20rpx;
}

.nav-btn-title {
  font-size: 30rpx;
  font-weight: 700;
  line-height: 1.1;
  color: #31445c;
}

.nav-btn-rank {
  background: linear-gradient(180deg, #f2fbff 0%, #e5f6ff 100%);
  border-color: rgba(0, 162, 194, 0.14);
}

.nav-btn-rank .nav-btn-title {
  color: #4a8192;
}

.nav-btn-history {
  background: linear-gradient(180deg, #f7f4ff 0%, #efebff 100%);
  border-color: rgba(132, 118, 230, 0.14);
}

.nav-btn-history .nav-btn-title {
  color: #6c63a8;
}

.nav-btn-active {
  transform: translateY(-1rpx);
}

.nav-btn-rank-active {
  background: linear-gradient(180deg, #eefaff 0%, #dff4ff 100%);
  border-color: rgba(0, 162, 194, 0.22);
  box-shadow: 0 8rpx 20rpx rgba(0, 162, 194, 0.12);
}

.nav-btn-rank-active .nav-btn-title {
  color: #0f6e82;
}

.nav-btn-history-active {
  background: linear-gradient(180deg, #f5f3ff 0%, #ece8ff 100%);
  border-color: rgba(132, 118, 230, 0.2);
  box-shadow: 0 8rpx 20rpx rgba(132, 118, 230, 0.12);
}

.nav-btn-history-active .nav-btn-title {
  color: #5a4db0;
}

.nav-btn-record {
  background: linear-gradient(135deg, #ffd86d 0%, #ffb300 100%);
  border-color: rgba(214, 144, 0, 0.2);
  box-shadow: 0 8rpx 20rpx rgba(255, 183, 77, 0.2);
}

.nav-btn-record .nav-btn-title {
  color: #fff;
}

.nav-btn-record-active {
  box-shadow: 0 10rpx 24rpx rgba(255, 183, 77, 0.28);
}

.nav-btn-squat {
  background: linear-gradient(135deg, #7fe7c4 0%, #16c784 100%);
  border-color: rgba(20, 167, 108, 0.22);
  box-shadow: 0 8rpx 20rpx rgba(22, 199, 132, 0.18);
}

.nav-btn-squat .nav-btn-title {
  color: #fff;
}

.nav-btn-squat-active {
  box-shadow: 0 10rpx 24rpx rgba(22, 199, 132, 0.28);
}

.profile-drawer-container {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 12;
}

.profile-drawer-mask {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(15, 23, 42, 0.45);
}

.profile-drawer {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  flex-direction: column;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(247, 250, 255, 0.99) 100%);
  border-radius: 32rpx 32rpx 0 0;
  box-shadow: 0 -14rpx 42rpx rgba(49, 68, 92, 0.18);
  padding: 12rpx 18rpx calc(14rpx + env(safe-area-inset-bottom));
}

.profile-drawer-handle {
  width: 88rpx;
  height: 10rpx;
  border-radius: 999rpx;
  background: rgba(126, 159, 209, 0.35);
  margin: 4rpx auto 14rpx;
}

.profile-drawer-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 6rpx;
}

.profile-drawer-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #1f2f45;
}

.profile-drawer-close {
  margin-left: 16rpx;
  padding: 10rpx 18rpx;
  border-radius: 999rpx;
  background: rgba(74, 144, 226, 0.08);
  border: 1rpx solid rgba(126, 159, 209, 0.16);
  color: #4a8192;
  font-size: 22rpx;
  font-weight: 600;
}

.profile-drawer-body {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 28rpx 12rpx 24rpx;
}

.profile-avatar-btn {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  overflow: hidden;
  padding: 0;
  margin: 0;
  background: transparent;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
}

.profile-avatar-btn::after {
  border: none;
}

.profile-avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.profile-avatar-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 80rpx;
  color: rgba(255, 255, 255, 0.4);
  background-color: rgba(255, 255, 255, 0.12);
}

.profile-avatar-tip {
  color: rgba(79, 99, 124, 0.72);
  font-size: 24rpx;
  margin-top: 16rpx;
  margin-bottom: 36rpx;
}

.profile-input-box {
  width: 100%;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.9);
  border: 2rpx solid rgba(126, 159, 209, 0.24);
  box-shadow: 0 8rpx 22rpx rgba(16, 31, 54, 0.08);
  overflow: hidden;
}

.profile-nickname-input {
  width: 100%;
  height: 88rpx;
  background-color: rgba(255, 255, 255, 0.72);
  border-radius: 18rpx;
  color: #31445c;
  font-size: 30rpx;
  padding: 0 30rpx;
  box-sizing: border-box;
}

.profile-nickname-placeholder {
  color: rgba(81, 104, 130, 0.65);
}

.profile-drawer-footer {
  display: flex;
  align-items: center;
  padding: 0 2rpx;
}

.profile-footer-btn {
  flex: 1;
  min-height: 72rpx;
  padding: 10rpx 14rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 999rpx;
  font-size: 30rpx;
  font-weight: 700;
  box-sizing: border-box;
}

.profile-footer-btn + .profile-footer-btn {
  margin-left: 20rpx;
}

.profile-footer-btn-cancel {
  background: linear-gradient(180deg, #f2fbff 0%, #e5f6ff 100%);
  border: 1rpx solid rgba(0, 162, 194, 0.14);
  color: #4a8192;
  box-shadow: 0 4rpx 12rpx rgba(86, 115, 156, 0.06);
}

.profile-footer-btn-confirm {
  background: linear-gradient(135deg, #ffd86d 0%, #ffb300 100%);
  border: 1rpx solid rgba(214, 144, 0, 0.2);
  color: #fff;
  box-shadow: 0 8rpx 20rpx rgba(255, 183, 77, 0.2);
}

.squat-popup-container {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 13;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx;
  box-sizing: border-box;
}

.squat-popup-mask {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(15, 23, 42, 0.48);
}

.squat-popup {
  position: relative;
  width: 100%;
  max-width: 686rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx 32rpx 32rpx;
  border-radius: 28rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(244, 252, 248, 0.99) 100%);
  border: 1rpx solid rgba(20, 167, 108, 0.12);
  box-shadow: 0 18rpx 48rpx rgba(21, 63, 42, 0.2);
  box-sizing: border-box;
}

.squat-popup-handle {
  width: 96rpx;
  height: 10rpx;
  border-radius: 999rpx;
  background: rgba(22, 199, 132, 0.2);
  margin-bottom: 28rpx;
}

.squat-popup-title {
  font-size: 32rpx;
  font-weight: 700;
  line-height: 1.3;
  color: #28543d;
}

.squat-popup-count {
  margin-top: 24rpx;
  font-size: 144rpx;
  font-weight: 700;
  line-height: 1;
  color: #16a76c;
}

.squat-popup-count-hit {
  color: #ff9f1a;
}

.squat-popup-tip {
  margin-top: 24rpx;
  font-size: 24rpx;
  line-height: 1.6;
  color: #5d7a69;
  text-align: center;
}

.squat-popup-tip-highlight {
  color: #16a76c;
  font-weight: 700;
}

.squat-popup-tip-highlight-hit {
  color: #ff9f1a;
}

.squat-popup-footer {
  width: 100%;
  display: flex;
  align-items: center;
  margin-top: 36rpx;
}

.squat-popup-btn {
  min-height: 76rpx;
  padding: 10rpx 14rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  box-sizing: border-box;
  font-size: 30rpx;
  font-weight: 700;
}

.squat-popup-btn-redeem {
  width: 100%;
  background: linear-gradient(135deg, #7fe7c4 0%, #16c784 100%);
  border: 1rpx solid rgba(20, 167, 108, 0.22);
  color: #fff;
  box-shadow: 0 8rpx 20rpx rgba(22, 199, 132, 0.2);
}

.squat-popup-btn-disabled {
  opacity: 0.7;
}

.squat-confirm-container {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 14;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx;
  box-sizing: border-box;
}

.squat-confirm-mask {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(15, 23, 42, 0.56);
}

.squat-confirm {
  position: relative;
  width: 100%;
  max-width: 622rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 32rpx 32rpx;
  border-radius: 28rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(255, 250, 239, 0.99) 100%);
  border: 1rpx solid rgba(255, 186, 73, 0.2);
  box-shadow: 0 18rpx 48rpx rgba(88, 67, 24, 0.18);
  box-sizing: border-box;
}

.squat-confirm-icon {
  width: 88rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(135deg, #ffe18d 0%, #ffb300 100%);
  color: #fff;
  font-size: 40rpx;
  font-weight: 700;
  box-shadow: 0 10rpx 24rpx rgba(255, 179, 0, 0.22);
}

.squat-confirm-title {
  margin-top: 24rpx;
  font-size: 34rpx;
  font-weight: 700;
  line-height: 1.5;
  color: #3f4c39;
  text-align: center;
}

.squat-confirm-amount {
  color: #16a76c;
}

.squat-confirm-amount-hit {
  color: #ff9f1a;
}

.squat-confirm-tip {
  margin-top: 20rpx;
  font-size: 26rpx;
  line-height: 1.6;
  color: #c88712;
  text-align: center;
}

.squat-confirm-tip-hit {
  color: #ff9f1a;
  font-weight: 700;
}

.squat-confirm-footer {
  width: 100%;
  display: flex;
  align-items: center;
  margin-top: 36rpx;
}

.squat-confirm-btn {
  flex: 1;
  min-height: 76rpx;
  padding: 10rpx 14rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  box-sizing: border-box;
  font-size: 30rpx;
  font-weight: 700;
}

.squat-confirm-btn + .squat-confirm-btn {
  margin-left: 20rpx;
}

.squat-confirm-btn-cancel {
  background: rgba(255, 179, 0, 0.08);
  border: 1rpx solid rgba(255, 179, 0, 0.16);
  color: #b47a0e;
}

.squat-confirm-btn-confirm {
  background: linear-gradient(135deg, #7fe7c4 0%, #16c784 100%);
  border: 1rpx solid rgba(20, 167, 108, 0.22);
  color: #fff;
  box-shadow: 0 8rpx 20rpx rgba(22, 199, 132, 0.2);
}
