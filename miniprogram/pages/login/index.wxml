<view class="page">
  <image class="page-bg" src="/images/background.png" mode="aspectFill" />
  <view class="page-bg-mask"></view>
  <view class="container">
    <view class="login-box">
      <button class="avatar-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image wx:if="{{userInfo.avatarUrl}}" class="avatar" src="{{userInfo.avatarUrl}}"></image>
        <view wx:else class="avatar-placeholder">+</view>
      </button>
      <text class="avatar-tip">点击选择头像</text>
      
      <view class="input-box">
        <input
          type="nickname"
          class="nickname-input"
          placeholder="请输入昵称，最多4字"
          placeholder-class="nickname-placeholder"
          maxlength="4"
          bindinput="onNicknameInput"
          value="{{userInfo.nickName}}"
        />
      </view>
      
      <button class="login-btn" bindtap="login" disabled="{{!canLogin}}">进入</button>
    </view>
  </view>
</view>
