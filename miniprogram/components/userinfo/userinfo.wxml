<!-- components/userinfo/userinfo.wxml -->
<view class="userinfo-container">
  <view class="profile-card">
    <view class="profile-main">
      <view class="avatar-wrap">
        <image class="avatar" src="{{avatar}}" />
      </view>

      <view class="info-right">
        <view class="profile-header">
          <view class="identity-block">
            <text class="username">{{username}}</text>
          </view>

          <view class="points-box">
            <view class="points-meta">
              <view class="coin-icon">
                <view class="coin-icon-inner"></view>
              </view>
              <text class="points-label">金币</text>
            </view>
            <text class="points">{{points}}</text>
          </view>

          <view class="shop-entry" catch:tap="onShopClick">
            <text class="shop-entry__label">商店</text>
          </view>

          <view class="settings-btn" catch:tap="onSettingsClick">
            <image class="gear-icon" src="/images/gears.png" />
          </view>
        </view>

        <view class="stats-panel">
          <view class="stat-item">
            <text class="stat-label">总场次</text>
            <text class="stat-value">{{totalGames}}</text>
          </view>
          <view class="stat-item">
            <text class="stat-label">胜场</text>
            <text class="stat-value">{{winCount}}</text>
          </view>
          <view class="stat-item stat-item-highlight {{winRateNegative ? 'stat-item-negative' : ''}}">
            <text class="stat-label">胜率</text>
            <text class="stat-value stat-value-highlight {{winRateNegative ? 'stat-value-negative' : ''}}">{{winRateText}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
